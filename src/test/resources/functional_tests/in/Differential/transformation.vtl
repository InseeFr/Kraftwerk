MULTIMODE := MULTIMODE [calc BOUCLEINDIV.TOKEEP_TRANSFORMATION := if BOUCLEINDIV.ETAT_SANT = "2" then 1 else 0];
MULTIMODE := MULTIMODE [calc TOKEEP_TRANSFORMATION_RACINE := if NHAB > 2.0 then 1 else 0];